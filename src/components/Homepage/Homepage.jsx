import styles from './Homepage.module.css'

const HomePage = () => {
    return (
        <div className={styles.container}>
            <h1>Tomi's Shop Epic</h1>
            <p>If we don't have it. Nobody does.</p>
        </div>
    )
}

export default HomePage;